# Development

BUILD AND TEST

  cargo build --release
  cargo test --workspace            host, transport and the command line
  cargo clippy --workspace --all-targets
  cargo fmt --all

The host tests start real nodes from the built `cartridge` binary: Lua
cartridges, a Rust cartridge as a native module built from
.cartridge/tests/unit/src/tests/fixtures/native, and a helper program started
with `cartridge.spawn`. Nodes run inside the sandbox, so the tests exercise it too.
The tests point `$CARTRIDGE_HOME` at a temp directory and record trust for
each temp project; nothing skips the check.

The security properties have their own regression tests: the sandbox denying
a write and an exec the grant did not name, one cartridge's globals being
invisible to another, the socket directory and socket modes, and the owner,
type and peer checks on both ends of a connection. The macOS-only ones are
cfg-gated.

RUNNING

  cartridge --dir <cartridge root> run <event> '<json>'
  cartridge --dir <cartridge root> daemon

The profile is the nearest .cartridge/init.lua at or above the working
directory. Run `cartridge trust` once per checkout, and again after changing
`.cartridge/init.lua`, `.cartridge/config.lua`, a `cartridge.json` or a
cartridge's Lua. A linked cartridge outside the project is trusted on its
own: `cartridge trust <its folder>`. `cartridge socket` prints the project's host socket; `cartridge
status`, `call`, `send`, `follow`, `reload`, `stop` and `sweep` talk to the host
serving it. One host serves a project: `cartridge daemon` refuses when another
already does and `cartridge daemon --replace` takes over from it in place,
while `run`, `launch` and `mcp` attach to whichever host is there, starting
the daemon detached when none answers. They never compose on their own: a
socket that answers but errors fails the command, naming the socket. A tool
outside the runtime attaches with `cartridge run <event>` (or `cartridge call`)
and never starts `cartridge daemon` itself.

`cartridge stop` is the one way to stop the host. A daemon also exits when its
project directory is removed. A host that accepts but never answers is
recovered by `cartridge stop`, else by `kill <pid>`, where <pid> is the
numeric directory beside `$(cartridge socket)` — never by process name.
`cartridge mcp` hands this terminal's stdio to the cartridge listening to `mcp`,
for an MCP client such as `claude mcp add cartridge -- cartridge mcp`.

TROUBLESHOOTING

  cartridge status                  every cartridge, its state, error and what it waits for
  cartridge follow lifecycle        state changes as they happen
  cartridge follow <id>.error       failures of one cartridge's listeners
  cartridge doctor                  every cartridge's own answer to whether it is healthy
  cartridge list                    the profile and whom each cartridge's sends reach, cycles marked
  cartridge settings <id>           a cartridge's settled configuration
  cartridge trust --list            every directory this machine will run

A node that fails to start reports the last lines of its stderr in its error;
a cartridge refused at planning says which event it names that nobody declares. Everything a cartridge writes to stderr goes to the diagnostics file
named by CARTRIDGE_DIAGNOSTICS; CARTRIDGE_LOG selects the host's own log level.

A cartridge denied by the sandbox usually fails with "Operation not permitted"
on a path or program its `grant` does not name.

A listener that fails with "exceeded its budget of N Lua instructions" looped
in Lua; a node that exited with status 70 kept looping after it. Raise
host.lua_instruction_budget if the work is real.

A runtime that cannot read something it needs before `main` dies on a signal
with nothing on stderr — an interpreter's trap (SIGTRAP), dyld aborts (SIGABRT). On macOS,
`log stream --predicate 'senderImagePath CONTAINS "Sandbox"'` while the
cartridge starts names the denied path. On Linux the node's first stderr line
names the Landlock ABI it was confined at; below V9 a unix socket named by a
path is not among the things the kernel gates.

A command stopping with "is in no trusted project" or "has changed since it
was trusted" names a file to read before `cartridge trust`.
