# Trace

The trace identifier one unit of work carries across the runtimes cartridge spans, and the diagnostic stream it labels. Diagnostics are written by a thread of their own, so a slow disk never stalls the runtime.
