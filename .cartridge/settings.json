{
  "name": "host",
  "description": "The runtime host's own tunable values: the caps, timeouts and queue depths it spends on behalf of every cartridge it composes.",
  "settings": {
    "lifecycle_queue": {
      "type": "integer",
      "default": 4096,
      "min": 16,
      "max": 1048576,
      "doc": "Depth of the runtime's lifecycle broadcast."
    },
    "startup_timeout_secs": {
      "type": "integer",
      "default": 60,
      "min": 1,
      "max": 3600,
      "doc": "How long a cartridge has to serve and apply, and how long a cartridge retries reaching another."
    },
    "event_timeout_ms": {
      "type": "integer",
      "default": 60000,
      "min": 1,
      "max": 86400000,
      "doc": "How long a sender waits for each listener of an event that declares no `timeout_ms`."
    },
    "shutdown_timeout_secs": {
      "type": "integer",
      "default": 15,
      "min": 1,
      "max": 3600,
      "doc": "How long a disposed child gets to exit before it is reaped."
    },
    "verify_timeout_secs": {
      "type": "integer",
      "default": 300,
      "min": 1,
      "max": 86400,
      "doc": "How long `verify` waits for a composition to settle before calling it stuck."
    },
    "watch_debounce_ms": {
      "type": "integer",
      "default": 500,
      "min": 1,
      "max": 600000,
      "doc": "How long the source watcher gathers further changes before reloading."
    },
    "mcp_reply_queue": {
      "type": "integer",
      "default": 1024,
      "min": 1,
      "max": 1048576,
      "doc": "Replies the MCP server may have in flight on this terminal's stdio."
    },
    "proxy_key_bytes": {
      "type": "integer",
      "default": 32,
      "min": 16,
      "max": 4096,
      "doc": "Entropy in a minted proxy key, in bytes."
    },
    "sandbox_error_chars": {
      "type": "integer",
      "default": 512,
      "min": 40,
      "max": 65536,
      "doc": "Characters of a sandbox failure's first line carried into the error."
    },
    "lua_memory_bytes": {
      "type": "integer",
      "default": 1073741824,
      "min": 0,
      "max": 68719476736,
      "doc": "Bytes the host's Lua interpreter may allocate across every Lua cartridge; 0 is unbounded."
    },
    "diagnostics_max_bytes": {
      "type": "integer",
      "default": 67108864,
      "min": 4096,
      "max": 1073741824,
      "doc": "Cap on the diagnostics file, with one rotated generation. `CARTRIDGE_DIAGNOSTICS_MAX_BYTES` overrides it for one invocation."
    }
  }
}
