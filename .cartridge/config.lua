-- This project's configuration: one table per profile entry id, laid over
-- ~/.cartridge/config.lua field by field. The base's own keys are declared in
-- settings.json beside this file and are configured under `host`.
--
-- Nothing is pinned here, and nothing should be pinned that merely restates a
-- declared default: the pin is what stops a value following the declaration
-- when the declaration is raised.
return {}
