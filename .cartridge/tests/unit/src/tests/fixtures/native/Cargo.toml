[package]
name = "native_fixture"
version = "0.1.0"
edition = "2021"
publish = false
build = "build.rs"

[lib]
crate-type = ["cdylib"]

# A second, behaviourally different build of this same module: `twice` triples.
[features]
rebuilt = []

[dependencies]
mlua = { version = "0.12", features = ["lua54", "module"] }

[workspace]
